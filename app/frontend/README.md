# Frontend

React + Vite + JSX + Tailwind.

Planned pages:

```text
Login
Dashboard
Menu
Orders
New Order
Order Details
Settings
Setup
```

Keep data access behind a small API module so HTTP and desktop IPC can implement the same interface.
