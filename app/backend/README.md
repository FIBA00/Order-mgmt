# Backend

Placeholder for the small Express API.

Planned implementation:

```text
src/
  app.js
  server.js
  db/
  middleware/
  routes/
  services/
```

The first implementation can use SQLite so the same domain is easy to run locally. A SaaS branch/configuration can later use PostgreSQL.
